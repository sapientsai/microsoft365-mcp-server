{
  "name": "@sapientsai/ms-graph-core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Microsoft Graph plumbing (upload, tickets, OData/pagination, error + auth-strategy types) for the Graph MCP servers.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "validate": "ts-builds validate",
    "format": "ts-builds format",
    "format:check": "ts-builds format:check",
    "lint": "ts-builds lint",
    "lint:check": "ts-builds lint:check",
    "typecheck": "ts-builds typecheck",
    "test": "ts-builds test",
    "test:watch": "ts-builds test:watch",
    "test:coverage": "ts-builds test:coverage",
    "build": "ts-builds build",
    "dev": "ts-builds dev"
  },
  "prettier": "ts-builds/prettier",
  "dependencies": {
    "functype": "^1.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "ts-builds": "^3.4.1",
    "tsdown": "^0.22.14"
  },
  "engines": {
    "node": ">=22"
  }
}
